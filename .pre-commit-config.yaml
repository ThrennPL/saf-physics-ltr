repos:
  - repo: local
    hooks:
      - id: security-mcp-sanity
        name: mcp baseline drift gate
        entry: python tools/mcp_baseline.py check
        language: system
        pass_filenames: false
